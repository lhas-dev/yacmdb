<%= form_with(model: server_person) do |form| %>
  <% if server_person.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(server_person.errors.count, "error") %> prohibited this server_person from being saved:</h2>

      <ul>
        <% server_person.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :server_id, style: "display: block" %>
    <%= form.text_field :server_id %>
  </div>

  <div>
    <%= form.label :people_id, style: "display: block" %>
    <%= form.text_field :people_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
